memcpy .. memory + copy !

헤더파일 string.h를 include하면 사용할 수 있는 메모리 복사 함수이다. 
어떻게 사용하는지 알아보자. . 

void* memcpy(복사 받을 메모리 포인터, 복사할 메모리 포인터, 복사할 데이터의 바이트 단위 길이)

매우 간단하다..

주의점
1. char* 타입의 c언어 문자열 형태의 전체를 복사할 때에는 맨 뒤에 문자열의 끝을 알리는 "\0"도 포함되기 때문에 길이가 +1 된다.
2. 복사할 메모리랑 복사될 메모리가 겹쳐져 있다면 함수가 제대로 동작되지 않는다.
